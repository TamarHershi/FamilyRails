<%= form_for @toy do |f| %>
  <%= f.label :kind %>:
  <%= f.text_field :kind, :require => true %>
	<br>
  <%= f.label :color %>:
  <%= f.text_field :color %>

  <%= @toy.persisted? ? (f.submit value: "update toy", id: 'special-button') : (f.submit value: "add toy", id: 'special-button') %>
<% end %>
